# 92 — Current State (the actual checkpoint)

> Part of the [project memory](00_MASTER_CONTEXT.md). **This file was generated by directly running `git status`/`git rev-parse HEAD`/`git branch` against the live working tree at the time of writing — not reconstructed from memory.** If a future session finds this file stale, re-run those same commands and trust the live repository over this file.

## Checkpoint update — 2026-09-24 (supersedes the sections below where they conflict)

Verified live via `git rev-parse HEAD` / `git branch --show-current` / `git status --short`:

- HEAD: `1039d5ca292b6f82b87e489d74993b057f2efeec` ("first commit") on branch **`main`**. `apps/web` and `docs/project-memory` are now committed (commits `0d7b94e`, `a60c672`) — the "uncommitted" notes below are historical.
- **Training Recommendation Composition layer: IMPLEMENTED, uncommitted.** New package `packages/training-recommendation` (`@ipmat/training-recommendation`) plus six new read methods in `@ipmat/db`. See [37_TRAINING_RECOMMENDATION.md §23](37_TRAINING_RECOMMENDATION.md) for exactly what was built and the deliberate deviations from the design.
- Verification after this unit: **1189/1189 tests passing (128 test files)**; full-repo `npm run typecheck`, `npm run lint`, `npm run build` all clean. Still no live database and no live AI call — everything ran against in-memory doubles / fake `PrismaClient`.
- `apps/web` still uses its fixture-backed adapter; it has NOT been switched to the new composition layer (no API layer exists to do so).

## Latest commit

```
db9c764a3ed61c844d16156b78f32765f5e637ee
D-039 addendum: fix RepairPlan/Autopsy persistence fidelity
```

Branch: `master`. This is the last **committed** state.

## Working tree state at the time this memory system was created (verified via `git status --short`)

```
 M package-lock.json          (from `npm install` linking the new apps/web workspace)
?? .claude/                    (launch.json — dev-server config for training-playground + web, not app code)
?? apps/web/                   (the first vertical-slice student UI — uncommitted)
?? docs/project-memory/        (this memory system — uncommitted)
```

**`apps/web` is real, working, tested code that has never been committed.** Do not assume it exists just because a future `git log` doesn't show it — always check working-tree status directly.

## Verification status of the last COMMITTED checkpoint (D-039 addendum, `db9c764`)

- **1100/1100 tests passing** (122 test files, 23 workspaces) — this was the full-repo count immediately after the D-039 addendum commit.
- Full typecheck clean, full lint clean, full build clean — verified across every workspace, not just the touched ones.
- Migration `0007_repair_plan_persistence_fidelity` verified column-by-column against `schema.prisma`.
- Prisma client regenerated and confirmed in sync.
- **No live database was used or claimed at any point** — every check ran against `InMemoryXRepository` doubles or fake `PrismaClient` objects.

## Verification status including the UNCOMMITTED `apps/web` work (as of this memory's creation)

- **1106/1106 tests passing** (123 test files) — the 6 new tests are `apps/web`'s own adapter test suite.
- Typecheck (`@ipmat/web`): clean. Lint (full repo): clean. Build (`@ipmat/web`, `tsc --noEmit && vite build`): clean.
- `git diff --stat -- packages/ apps/training-playground/`: **empty** — confirmed zero modification to any decision-algorithm package or the playground.

## Latest completed architectural unit

**D-039 addendum — RepairPlan/Autopsy persistence fidelity.** Closed a genuine, previously-undiscovered gap: `toRepairPlanPersistenceRecord()` silently dropped six domain fields (`targetConceptName`, `targetPatternFamilyName`, `targetTaxonomyCellId`, `targetErrorCategory`, `recommendedTrainingMode`, `priority`); `Autopsy` had no column for the confirmation timestamp. Fixed via migration `0007`, a new `fromRepairPlanPersistenceRecord()` reverse mapping (fails closed — returns `null`, never fabricates, for any pre-migration-shaped row), and `RepairPlanRepository.findConfirmedActiveByStudentId()`. See [23_AUTOPSY.md](23_AUTOPSY.md), [24_REPAIR.md](24_REPAIR.md).

## Current next architectural unit

**Training Recommendation Composition Layer.** Design status: **IMPLEMENTATION READY: YES** (as of this session's third design pass, after the D-039 addendum unblocked it). See [37_TRAINING_RECOMMENDATION.md](37_TRAINING_RECOMMENDATION.md) for the complete, implementation-ready design — package location, dependency graph, exact new repository methods, composition flow, failure semantics, and two named out-of-scope items (PrepPhase/CatchUp assembly, deferred behind two real schema gaps). **Now implemented (2026-09-24, uncommitted) — see the checkpoint update at the top of this file.** At the time this section was written it had not been, because that session paused backend progression to build a visible first-slice UI instead (see below), per explicit user direction.

## The most recent, explicit pivot — and why it's recorded here plainly

The user's most recent substantive instruction was: **"I want to SEE the product now... start the REAL STUDENT-FACING WEB PRODUCT now."** This explicitly, deliberately deprioritized the backend-first sequence (`docs/MASTER_PLAN.md`'s own stated next-steps order: real-Anthropic smoke tests → re-validated content → Phase 4B-3+ → Phase 5C-3's UI) in favor of a fixture-backed, real-domain-logic-driven first vertical slice of the actual student experience. **This was not a silent architecture change** — it was an explicit, user-directed reprioritization, executed with the same strict scope/verification discipline as every other unit in this project (see [83_CLAUDE_CODE_WORKFLOW.md](83_CLAUDE_CODE_WORKFLOW.md)).

## `apps/web` — what it is, exactly, right now

A Vite + React SPA (port 5184), fixture-backed, calling **real, unmodified** domain functions (`@ipmat/attempt`, `@ipmat/autopsy`, `@ipmat/mastery`, `@ipmat/training-orchestration`, `@ipmat/ai`'s `FixtureProvider`) through a thin, replaceable adapter (`TrainingRecommendationAdapter`). Demonstrates the full flow: Dashboard → recommended training → question → timer → answer → result → autopsy → confirmation → next training. See [60_STUDENT_UX.md](60_STUDENT_UX.md) through [66_TRAINING_MODE_UX.md](66_TRAINING_MODE_UX.md) for full detail. **Not committed.** `apps/training-playground` (the internal dev tool) was explicitly, verifiably left unmodified throughout this work.

## Current product-visibility strategy (explicit, as directed)

- Training Playground exists (internal, developer-facing, unchanged).
- **The student-facing web product is a real first vertical slice, not yet complete** — no auth, no real persistence, no HTTP/API layer, fixture-backed session state only.
- The UI began before the entire backend MVP was finished — an explicit, deliberate choice, not an accident of sequencing.
- The UI remains a consumer of an application-facing adapter interface only — it does not duplicate business logic, and the adapter is designed to be replaced by the real Training Recommendation Composition layer without touching any component.

## Current scope status — what is and is NOT true, verified against the actual repository

**Do NOT claim, unless a future session re-verifies directly:**
- A production database (**false** — none has ever been reachable, ever, in this project's history).
- Live AI validation (**false** — no `ANTHROPIC_API_KEY` has ever been configured).
- A production API (**false** — no HTTP route of any kind exists anywhere).
- A production UI (**false** — `apps/web` is a first vertical slice, fixture-backed, uncommitted).
- Payments (**false** — not built, not designed, explicitly out of scope).
- Full mocks/Mock Simulation (**false** — undesigned).
- Revision (**false** — undesigned).
- Public launch (**false** — the go/no-go gate has not been met; see [03_MVP_SCOPE.md](03_MVP_SCOPE.md)).

**Verified true, as of this checkpoint:**
- 1106/1106 tests passing, 123 test files, 23+ workspaces, full typecheck/lint/build clean.
- All five training-system providers built, tested, and wired into orchestration.
- RepairPlan/Autopsy persistence round-trip fidelity fixed and verified (migration 0007).
- A real, working, tested, uncommitted first-slice student UI exists and was manually verified running at `http://localhost:5184/`.

See also: [99_SESSION_HANDOFF.md](99_SESSION_HANDOFF.md) for the concise next-session briefing.
